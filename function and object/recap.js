var skeyColor = "white";

var phones = ['iphone', 'xhomi', 'samsung', 'lg', 'htc'];
// phones[3] = 'walton';

// console.log(phones);

if (phones.indexOf('oppo' == -1)) {
    console.log(" sujan oppo arir khan khan is messing");
}

// lg is avable 
if (phones.indexOf("lg" != -1)) {
    console.log("lg is availabe now");
}

// var num = 0;

// for (var i = 0; num < phone.lenght; i++) {

// }
// function

function addThreeNumbers(number1, number2, number3) {
    var total = number1 + number2 + number3
    return total;
}

var result = addThreeNumbers(33, 33, 33);

console.log(result);

// object 

var microphone = {
    name: "blueyeti",
    pric: 120,
    color: black,

}